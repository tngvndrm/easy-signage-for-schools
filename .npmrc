# Fail install outright on a too-old Node (see "engines" in package.json)
# instead of npm's default warning — an old apt Node on the LAN host should
# stop the deploy at `npm ci`, not at a confusing runtime error.
engine-strict=true
